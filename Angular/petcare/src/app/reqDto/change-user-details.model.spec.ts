import { ChangeUserDetails } from './change-user-details.model';

describe('ChangeUserDetails', () => {
  it('should create an instance', () => {
    expect(new ChangeUserDetails()).toBeTruthy();
  });
});
