import { ApplicationDateDto } from './application-date-dto.model';

describe('ApplicationDateDto', () => {
  it('should create an instance', () => {
    expect(new ApplicationDateDto()).toBeTruthy();
  });
});
